import 'package:flutter/material.dart';

class myThemeData {
  static final ThemeData themeApp = ThemeData(
      primaryColor: const Color(0xFF274460),
      scaffoldBackgroundColor: const Color(0xFF274460),
      appBarTheme: const AppBarTheme(
        centerTitle: true,
      ));
}
